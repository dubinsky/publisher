# publisher
Generate, serve and publish websites

TODO parse Markdown and convert it to HTML!!

TODO CLI 
  - use ZIO CLI
  - set log level from the CLI option 
  - TODO checks:
  - links with http[s]://
  - broken links
  - inconsistent titles

TODO generate tags.html, sitemap.xml, robots.txt, feed.xml
TODO serve, auto-generation with filesystem watch
TODO push into a bucket
TODO package as a GitHub Action


TODO ZIO Blocks
- YAML Schema field names are hard-coded to use kebab case; I want camel!!
- clean up round-trips
- theming of Markdown -> HTML rendering
- XML pretty-printing: add mine?
- add XML SAX parsing
