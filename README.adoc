= Publisher

Generate, serve and publish websites

== TODO

Wiki links in Obsidian:
- are they case-sensitive?
- do they take into account file name, title property, document title?
- how do they look in the file if name reference is ambiguous?
- can they be split between the lines?
- do they support |?
- do they support any kind of agglutination?

FrontMatter: log warnings about malformed values

Calculate content structure for TOC etc.; add anchors - and not just in HTML?

Handle:
- permalinks and multi-name redirects
- raw TEI
- categories that are wiki links (as in Obsidian)

Config:
- figure out how to prevent `sourceDirectoryPath` from being written back
- get `Config.dailyNotesDirectoryName` out of `.obsidian/daily-notes.json/folder`
- make inclusion of `_drafts` configurable

CLI
- use ZIO CLI
- set log level from the CLI option
- serve with filesystem watch
- publish site into a bucket
- package as a stand-alone executable
- package as a GitHub Action

Generate:
- tags.html
- sitemap.xml
- robots.txt
- feed.xml

Checks/reports:
- broken links
- inconsistent titles

Additional markdown types:
- TEI!
- AsciiDoc?
- DocBook??

ZIO Blocks:
- YAML Schema field names are hard-coded to use kebab case;
  I want camel-case; it there a way to at least post-process the schema?
- where is ZIO Blocks's *native* support for the primitive type in Yaml parser?
- clean up round-trips in markdown tests
- theming of Markdown -> HTML rendering: useless; I need conversion to HTML AST, not text!
- XML pretty-printing: add mine?
- add XML SAX parsing
