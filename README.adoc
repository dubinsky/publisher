= Publisher

Generate, serve and publish websites.

== Introduction

- Jekyll finally pissed me off
- Support publishing of the Obsidian vault
- Generate static site for the alter-rebbe.org
- Publish blog as X Articles

Opinionated:

- no plugins
- no SCSS
- no template languages


== TODO

Config:

- get `Config.dailyNotesDirectoryName` out of `.obsidian/daily-notes.json/folder`
- make inclusion of `_drafts` configurable

Figure out how to deal with the HTML being split by the ZIO Blocks Markdown parser...
No wonder ZIO Blocks do not provide the Doc->Xml renderer, only the Doc->String one ;)

Anchors:

- calculate content structure for TOC etc.
- add section anchors if they are not there
- detect block anchors

Wiki links in Obsidian:

- are they case-sensitive?
- do they take into account file name, title property, document title?
- how do they look in the file if name reference is ambiguous?
- can they be split between the lines?
- do they support |?
- do they support any kind of agglutination?
- categories can be wiki links?!
- handle permalinks and multi-name redirects

CLI:

- use ZIO CLI
- set log level from the CLI option
- serve with filesystem watch
- publish site into a bucket
- package as a stand-alone executable
- package as a GitHub Action

Additional markup types:

- TEI (including facsimiles and raw TEI)
- AsciiDoc?
- DocBook??
- ReStructured Text?
